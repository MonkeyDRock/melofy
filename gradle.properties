# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m -Dfile.encoding=UTF-8

# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

# Enable AndroidX support
android.useAndroidX=true
# Disable Jetifier if you don't use legacy libraries (speeds up build)
android.enableJetifier=false

# Enable modern Gradle features for better performance and reliability
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.vfs.watch=true

# Disable Java Toolchain auto-detection to prevent Gradle from picking up stripped JREs (e.g. from extensions)
org.gradle.java.installations.auto-detect=false
